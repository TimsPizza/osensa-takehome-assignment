{
	admin off
	auto_https off
}

:8080 {
	root * /srv
	encode zstd gzip

	respond /healthz 200
	file_server

	header {
		X-Content-Type-Options "nosniff"
		X-Frame-Options "DENY"
		Referrer-Policy "strict-origin-when-cross-origin"
	}

	log {
		output stdout
		format json
	}
}
